interface:
  display_name: "Paper Metadata Normalizer"
  short_description: "Normalize paper metadata into meta.yaml"
  default_prompt: "Use $paper-metadata-normalizer to extract and clean the metadata for this paper into meta.yaml."
