# Copy this file to workflow/api_tokens.env and replace the placeholders.
# Keep the copied file local and untracked; never commit real credentials.

SEMANTIC_SCHOLAR_API_KEY=<your-semantic-scholar-api-key>
MINERU_API_TOKEN=<your-mineru-api-token>
